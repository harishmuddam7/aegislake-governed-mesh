name: AegisLake Automated PR Governance Gate

on:
  pull_request:
    branches: [ main, develop ]

jobs:
  audit-governance:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Run PR Gatekeeper Evaluation
        run: |
          python scripts/pr_gatekeeper_bot.py
